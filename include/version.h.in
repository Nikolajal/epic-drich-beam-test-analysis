#pragma once
//  ─────────────────────────────────────────────────────────────────────────
//  AUTO-GENERATED by CMake (configure_file) from
//      git describe --tags --always --dirty
//  at configure time.  DO NOT EDIT version.h — edit include/version.h.in.
//
//  Used to stamp every writer's output ROOT file (the Config/ TDirectory,
//  via util::ConfigDump) with the release the binary was built from, so an
//  output file always carries the provenance of the code that produced it.
//
//  Value examples:  "v2.1.0"  (built exactly on a tag)
//                   "v2.1.0-3-gabc1234"  (3 commits past v2.1.0)
//                   "v2.1.0-dirty"  (uncommitted tree)
//                   "abc1234"  (no tag reachable; --always fallback)
//  Stale-build note: regenerated at CMake *configure* time, so reconfigure
//  (or touch this template) after tagging to refresh a build's stamp.
//  ─────────────────────────────────────────────────────────────────────────
#define BTANA_VERSION "@BTANA_GIT_DESCRIBE@"
